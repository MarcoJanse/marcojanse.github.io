// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/jekyll
{
	"name": "Jekyll",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	"image": "mcr.microsoft.com/devcontainers/jekyll:bullseye",

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	"forwardPorts": [4000, 35729],

	// Open new integrated terminals already inside docs/, without changing what
	// VS Code treats as the workspace root — Explorer/search/git stay repo-wide.
	"customizations": {
		"vscode": {
			"settings": {
				"terminal.integrated.cwd": "${containerWorkspaceFolder}/docs"
			}
		}
	},

	// Install gems into the repo instead of /usr/local/bundle so new Codespaces stay writable.
	"postCreateCommand": "bash -lc 'cd docs && bundle config set --local path \"vendor/bundle\" && bundle config set --local cache_path \".bundle/cache\" && bundle install'"

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}
